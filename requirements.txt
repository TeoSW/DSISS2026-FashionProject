# --- scraping / dataset (existing main.py) ---
requests
beautifulsoup4
pillow
tqdm

# --- recognition pipeline ---
torch
transformers
rembg
onnxruntime
scipy          # mask repair in remove_bg; arrives with rembg, pinned here because we import it directly

# --- knowledge graph ---
neo4j

# --- http api for the front end (api.py) ---
fastapi
uvicorn[standard]
python-multipart

# --- config ---
python-dotenv
