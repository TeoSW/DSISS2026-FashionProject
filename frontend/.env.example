# Copy to .env.local and edit if the backend is not on this machine.
# A tunnel address (cloudflared tunnel --url http://localhost:8000) goes here
# when the page is served from anywhere other than localhost.
VITE_API_URL=http://localhost:8000
